@model IEnumerable<prjCustomer.Models.tCustomer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CCity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CPhone)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CCity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CPhone)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { CId = item.CId }) |
            @Html.ActionLink("Details", "Details", new { CId = item.CId }) |
            @Html.ActionLink("Delete", "Delete", new { CId = item.CId })
        </td>
    </tr>
}

</table>
